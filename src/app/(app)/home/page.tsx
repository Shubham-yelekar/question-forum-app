import QuestionCard from "@/components/QuestionCard";
import React from "react";

const page = () => {
  return (
    <div>
      <QuestionCard questionData={""} />
    </div>
  );
};

export default page;
